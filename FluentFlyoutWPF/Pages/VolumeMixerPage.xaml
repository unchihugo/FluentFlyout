<Page x:Class="FluentFlyoutWPF.Pages.VolumeMixerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
      xmlns:utils="clr-namespace:FluentFlyoutWPF.Classes.Utils"
      xmlns:fluentflyoutcontrols="clr-namespace:FluentFlyout.Controls"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="800"
      Title="VolumeFlyoutPage"
      d:DataContext="{d:DesignInstance viewModels:UserSettings}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      d:Background="White">

    <Page.Resources>
        <utils:BoolToVisibleCollapsedConverter x:Key="BoolToCollapsedVisibleConverter" TrueValue="Collapsed" FalseValue="Visible" />
        <utils:BoolToFullHalfOpacityConverter x:Key="BoolToFullHalfOpacityConverter" />
    </Page.Resources>

    <StackPanel MaxWidth="1000" Margin="16">
        <TextBlock Text="{DynamicResource VolumeFlyoutTitle}" FontSize="24" FontWeight="SemiBold" FontFamily="Segoe UI Variable" Margin="0,0,0,20" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                <Border CornerRadius="10" ClipToBounds="True" Width="164" Height="98">
                    <Border.Background>
                        <ImageBrush ImageSource="/Resources/FluentFlyoutVolumeDemo.png" Stretch="UniformToFill" />
                    </Border.Background>
                </Border>
                <TextBlock FontSize="14" FontWeight="Regular" HorizontalAlignment="Stretch" Opacity="0.75" Margin="12,0,0,0" MaxWidth="450" TextWrapping="WrapWithOverflow"
                           Text="{DynamicResource VolumeFlyoutDescription}" />
            </StackPanel>
        </Grid>

        <StackPanel>
            <ui:CardControl Icon="{ui:SymbolIcon Symbol=Speaker224}" Margin="0,0,0,24">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource EnableVolumeFlyoutTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                            <ui:InfoBadge Value=" NEW " FontWeight="Medium" FontSize="10" Margin="2,-2,0,-2" Severity="Success" />
                        </StackPanel>
                        <TextBlock Text="{DynamicResource EnableVolumeFlyoutDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <controls:ToggleSwitch IsChecked="{Binding VolumeControlEnabled, Mode=TwoWay}" />
            </ui:CardControl>

            <ui:CardControl Icon="{ui:SymbolIcon ArrowMove24}" Margin="0,0,0,3">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{DynamicResource VolumeAboveMediaFlyoutTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                    </StackPanel>
                </ui:CardControl.Header>
                <controls:ToggleSwitch IsChecked="{Binding VolumeControlAboveMediaFlyout, Mode=TwoWay}" />
            </ui:CardControl>

            <ui:CardControl Grid.Row="3" Icon="{ui:SymbolIcon Timer24}" Margin="0,0,0,12">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{DynamicResource VolumeFlyoutStayDurationTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="{DynamicResource FlyoutStayDurationDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <StackPanel Orientation="Horizontal">
                    <ui:TextBox Width="60" ClearButtonEnabled="False" Text="{Binding VolumeControlDurationText, Mode=TwoWay}" />
                    <TextBlock Text="{DynamicResource MillisecondsUnit}" FontSize="14" FontWeight="Regular" Margin="10,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
            </ui:CardControl>

            <TextBlock Text="{DynamicResource VolumeMixerSectionTitle}" FontSize="14" FontWeight="SemiBold" Margin="0,12,0,6" />
            <ui:CardControl Icon="{ui:SymbolIcon SpeakerSettings24}" Margin="0,0,0,3">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{DynamicResource EnableVolumeMixerTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="{DynamicResource EnableVolumeMixerDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <controls:ToggleSwitch IsChecked="{Binding VolumeMixerEnabled, Mode=TwoWay}" />
            </ui:CardControl>
            
            <ui:CardControl Icon="{ui:SymbolIcon Highlight24}" Margin="0,0,0,76" Opacity="{Binding IsPremiumUnlocked, Converter={StaticResource BoolToFullHalfOpacityConverter}}">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource VolumeMixerHighlightTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                            <fluentflyoutcontrols:AccentBadge Margin="6,0,0,0" />
                        </StackPanel>
                        <TextBlock Text="{DynamicResource VolumeMixerHighlightDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <controls:ToggleSwitch IsChecked="{Binding VolumeMixerHighlightActiveApps, Mode=TwoWay}" />
            </ui:CardControl>

            <!-- Ignored Audio Sources -->
            <TextBlock Text="{DynamicResource IgnoredAudioSourcesSectionTitle}" FontSize="14" FontWeight="SemiBold" Margin="0,4,0,6" />

            <ui:CardExpander IsExpanded="True" Margin="0,0,0,76">
                <ui:CardExpander.Header>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon Symbol="EyeOff24" FontSize="20" Margin="0,0,16,0" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="{DynamicResource IgnoredAudioSourcesCardTitle}" FontSize="14" FontWeight="Regular" />
                            <TextBlock Text="{DynamicResource IgnoredAudioSourcesCardDescription}" FontSize="12" Opacity="0.5" TextWrapping="Wrap" Margin="0,2,0,0" />
                        </StackPanel>
                    </Grid>
                </ui:CardExpander.Header>

                <StackPanel Margin="0">
                    <!-- Dropdown: pick from currently active audio sessions -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Margin="0,0,8,0">
                            <ComboBox x:Name="IgnoredSourceComboBox"
                                      HorizontalAlignment="Stretch"
                                      DropDownOpened="IgnoredSourceComboBox_DropDownOpened"
                                      MinHeight="36" Padding="16,8,16,8">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                                        <Setter Property="Padding" Value="16,8,16,8" />
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>
                            <TextBlock Text="{DynamicResource AppFilteringComboBoxHint}"
                                       Margin="16,0,32,0" VerticalAlignment="Center"
                                       IsHitTestVisible="False" Opacity="0.5">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedItem, ElementName=IgnoredSourceComboBox}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                        <ui:Button Grid.Column="1" Content="{DynamicResource AddButton}"
                                   Click="AddIgnoredSource_Click" Appearance="Primary" />
                    </Grid>

                    <!-- List of currently ignored sources -->
                    <ListBox ItemsSource="{Binding IgnoredAudioSources}" MinHeight="80"
                             HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                                <Setter Property="Focusable" Value="False" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Stretch">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding}" VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis" />
                                    <ui:Button Grid.Column="1" Content="{DynamicResource RemoveButton}"
                                               Click="RemoveIgnoredSource_Click" Tag="{Binding}"
                                               Margin="8,0,0,0" Padding="8,4" Background="Transparent"
                                               HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Manual entry -->
                    <StackPanel Margin="0,16,0,0">
                        <Border Height="1" Background="{DynamicResource CardStrokeColorDefaultBrush}" Margin="0,0,0,12" />
                        <TextBlock Text="{DynamicResource AddAppManuallyText}" FontSize="12" Opacity="0.65" Margin="0,0,0,8" />
                        <TextBlock Text="{DynamicResource IgnoredAudioSourcesBrowserNote}"
                                   FontSize="11" Opacity="0.45" TextWrapping="Wrap" Margin="0,0,0,8" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ui:TextBox Grid.Column="0" x:Name="IgnoredSourceTextBox"
                                        PlaceholderText="{DynamicResource IgnoredAudioSourcesPlaceholder}"
                                        HorizontalAlignment="Stretch" Margin="0,0,8,0" ClearButtonEnabled="False" />
                            <ui:Button Grid.Column="1" Content="{DynamicResource AddButton}"
                                       Click="AddIgnoredSourceManual_Click" Appearance="Secondary" />
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </ui:CardExpander>
        </StackPanel>
    </StackPanel>
</Page>