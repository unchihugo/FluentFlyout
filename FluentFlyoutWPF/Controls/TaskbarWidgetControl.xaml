<UserControl x:Class="FluentFlyout.Controls.TaskbarWidgetControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:UserSettings}"
             Height="40" Width="100"
             TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Ideal"
             FontFamily="{Binding FontFamily}"
             FlowDirection="{Binding FlowDirection}">
    <Border Name="MainBorder" CornerRadius="6" MouseEnter="Grid_MouseEnter" MouseLeave="Grid_MouseLeave" MouseLeftButtonUp="Grid_MouseLeftButtonUp" ClipToBounds="True">
        <Grid>
            <Image x:Name="BackgroundImage" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5" Opacity="0.4">
                <Image.Effect>
                    <BlurEffect Radius="80" />
                </Image.Effect>
            </Image>
            <Border Name="TopBorder" BorderThickness="0,1.25,0,0" CornerRadius="5" Margin="0,0,0,1.25">
                <Grid Name="MainGrid" HorizontalAlignment="Left" VerticalAlignment="Top" RenderTransformOrigin="0,0.5">
                    <Grid.RenderTransform>
                        <ScaleTransform ScaleX="0.9" ScaleY="0.9" />
                    </Grid.RenderTransform>
                    <StackPanel Name="MainStackPanel" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Width="Auto" Margin="4,0,-100,0">
                        <Border Name="SongImageBorder" CornerRadius="5" ClipToBounds="True" Width="36" Height="36" Panel.ZIndex="2">
                            <Grid>
                                <ui:SymbolIcon Name="SongImagePlaceholder" Symbol="MusicNote220" FontSize="24" Filled="True" Foreground="{DynamicResource MicaWPF.Brushes.SystemAccentColorTertiary}" />
                            </Grid>
                            <Border.Background>
                                <ImageBrush x:Name="SongImage" Stretch="UniformToFill" />
                            </Border.Background>
                        </Border>

                        <StackPanel Name="SongInfoStackPanel" Orientation="Vertical" VerticalAlignment="Center" Width="Auto" Margin="8,0,0,0" 
                                    ToolTipService.InitialShowDelay="800"
                                    ToolTipService.Placement="RelativePoint"
                                    ToolTipService.VerticalOffset="-16">
                            <Canvas x:Name="SongTitleContainer" ClipToBounds="True" Height="19">
                                <TextBlock x:Name="SongTitle" Text="" FontWeight="Normal">
                                    <TextBlock.RenderTransform>
                                        <TranslateTransform />
                                    </TextBlock.RenderTransform>
                                </TextBlock>
                            </Canvas>
                            <Canvas x:Name="SongArtistContainer" ClipToBounds="True" Margin="0,-1.5,0,0" Height="19">
                                <TextBlock x:Name="SongArtist" Text="" FontWeight="Normal" Opacity="0.5">
                                    <TextBlock.RenderTransform>
                                        <TranslateTransform />
                                    </TextBlock.RenderTransform>
                                </TextBlock>
                            </Canvas>
                        </StackPanel>

                        <StackPanel Name="ControlsStackPanel" Orientation="Horizontal" VerticalAlignment="Center" Width="Auto">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TaskbarWidgetControlsEnabled}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <ui:Button Name="PreviousButton" Appearance="Transparent" Icon="{ui:SymbolIcon Previous20, Filled=True}" Height="32" Width="32" Padding="0" BorderThickness="0" Margin="0,0,0,-1" Click="Previous_Click"/>
                            <ui:Button Name="PlayPauseButton" Appearance="Transparent" Icon="{ui:SymbolIcon Pause16, Filled=True}" Height="32" Width="32" Padding="0" BorderThickness="0" Margin="0,0,0,-1" Click="PlayPause_Click"/>
                            <ui:Button Name="NextButton" Appearance="Transparent" Icon="{ui:SymbolIcon Next20, Filled=True}" Height="32" Width="32" Padding="0" BorderThickness="0" Margin="0,0,0,-1" Click="Next_Click"/>
                        </StackPanel>

                    </StackPanel>
                </Grid>
            </Border>

            <Grid Name="SeekBarTrack" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                  Width="{Binding ActualWidth, ElementName=TopBorder}" Height="12" Margin="0,0,0,0"
                  Background="Transparent" Cursor="Hand" Panel.ZIndex="3"
                  MouseLeftButtonDown="SeekBarTrack_MouseLeftButtonDown"
                  MouseMove="SeekBarTrack_MouseMove"
                  MouseLeftButtonUp="SeekBarTrack_MouseLeftButtonUp">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding TaskbarWidgetSeekbarEnabled}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid VerticalAlignment="Center" Height="6" IsHitTestVisible="False">
                    <Border Background="#33FFFFFF" CornerRadius="3" />
                    <Rectangle Name="SeekBarFill" HorizontalAlignment="Left" Width="0" RadiusX="3" RadiusY="3"
                               Fill="{DynamicResource MicaWPF.Brushes.SystemAccentColorTertiary}" />
                </Grid>
                <Ellipse Name="SeekBarThumb" Width="12" Height="12" HorizontalAlignment="Left" VerticalAlignment="Center"
                         Fill="{DynamicResource MicaWPF.Brushes.SystemAccentColorTertiary}" IsHitTestVisible="False">
                    <Ellipse.Effect>
                        <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.5" Color="Black" />
                    </Ellipse.Effect>
                    <Ellipse.RenderTransform>
                        <TranslateTransform x:Name="SeekBarThumbTransform" X="-6" />
                    </Ellipse.RenderTransform>
                </Ellipse>
            </Grid>
        </Grid>
    </Border>
</UserControl>