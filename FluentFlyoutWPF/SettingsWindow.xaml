<controls:MicaWindow x:Class="FluentFlyoutWPF.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:FluentFlyoutWPF"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
    mc:Ignorable="d" 
    Height="700" MinHeight="300" MinWidth="660" SizeToContent="Width" 
    Title="FluentFlyout" Icon="/Resources/FluentFlyout2.ico"
    TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Ideal"
    FontFamily="Segoe UI Variable, Microsoft YaHei, Microsoft JhengHei, MS Gothic"
    Closing="SettingsWindow_Closing"
    xmlns:System="clr-namespace:System;assembly=System.Runtime"
    xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:UserSettings}"
    TitleBarType="WinUI"
    >
    <Grid>
        <ui:PassiveScrollViewer HorizontalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="1000">
                <StackPanel Orientation="Vertical" Margin="16,16,16,36">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                        <Image Source="/Resources/FluentFlyout2.ico" Width="44" Height="44"/>
                        <TextBlock Text="{DynamicResource TitleText}" FontSize="24" FontWeight="Medium" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="VersionTextBlock" FontSize="14" FontWeight="Regular" VerticalAlignment="Bottom" Margin="10" Opacity="0.5"/>
                    </StackPanel>
                    <TextBlock Text="{DynamicResource MediaFlyoutTitle}" FontSize="20" FontWeight="SemiBold" FontFamily="Segoe UI Variable" Margin="0,48,0,0"/>
                    <Grid Margin="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                            <Border CornerRadius="10" ClipToBounds="True" Width="164" Height="98">
                                <Border.Background>
                                    <ImageBrush ImageSource="/Resources/FluentFlyoutDemo3.1.png" Stretch="UniformToFill"/>
                                </Border.Background>
                            </Border>
                            <TextBlock Text="{DynamicResource MediaFlyoutDescription}" FontSize="14" FontWeight="Regular" HorizontalAlignment="Stretch" Opacity="0.75" Margin="12,0,0,0" MaxWidth="450" TextWrapping="WrapWithOverflow" />
                        </StackPanel>
                        <ui:CardControl Grid.Row="1" Icon="{ui:SymbolIcon VideoPlayPause24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource TextBlockText1}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource TextBlockText3}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="MediaFlyoutEnabledSwitch" IsChecked="{Binding MediaFlyoutEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="2" Icon="{ui:SymbolIcon ColorBackground24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource BackgroundBlurTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource TextBlockText2}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <ComboBox Name="BackgroundComboBox" SelectedIndex="{Binding MediaFlyoutBackgroundBlur}">
                                <ComboBoxItem Content="{DynamicResource BackgroundBlurOption1}"/>
                                <ComboBoxItem Content="{DynamicResource BackgroundBlurOption2}"/>
                                <ComboBoxItem Content="{DynamicResource BackgroundBlurOption3}"/>
                                <ComboBoxItem Content="{DynamicResource BackgroundBlurOption4}"/>
                            </ComboBox>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="3" Icon="{ui:SymbolIcon Blur24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource AcrylicWindowTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource AcrylicWindowDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="AcrylicWindowSwitch" IsChecked="{Binding MediaFlyoutAcrylicWindowEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="4" Icon="{ui:SymbolIcon ArrowAutofitWidth24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource CompactLayoutTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource CompactLayoutDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="LayoutSwitch" IsChecked="{Binding CompactLayout, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="5" Icon="{ui:SymbolIcon ArrowMove24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource FlyoutPositionTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <ComboBox Name="PositionComboBox" SelectedIndex="{Binding Position, Mode=TwoWay}" >
                                <ComboBoxItem Content="{DynamicResource PositionBottomLeft}"/>
                                <ComboBoxItem Content="{DynamicResource PositionBottomCenter}"/>
                                <ComboBoxItem Content="{DynamicResource PositionBottomRight}"/>
                                <ComboBoxItem Content="{DynamicResource PositionTopLeft}"/>
                                <ComboBoxItem Content="{DynamicResource PositionTopCenter}"/>
                                <ComboBoxItem Content="{DynamicResource PositionTopRight}"/>
                            </ComboBox>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="6" Icon="{ui:SymbolIcon Timer24}" Margin="0,0,0,12">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource FlyoutStayDurationTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource FlyoutStayDurationDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource MediaFlyoutAlwaysDisplay}" VerticalAlignment="Center"/>
                                <controls:ToggleSwitch Margin="4,0,12,0" IsChecked="{Binding MediaFlyoutAlwaysDisplay, Mode=TwoWay}"/>
                                <ui:TextBox Name="DurationTextBox" Width="60" ClearButtonEnabled="False" Text="{Binding DurationText, Mode=TwoWay}" IsEnabled="{Binding IsDurationEditable, Mode=OneWay}"/>
                                <TextBlock Text="{DynamicResource MillisecondsUnit}" FontSize="14" FontWeight="Regular" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="7" Icon="{ui:SymbolIcon AlignLeft24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource CenterTitleArtistTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource CenterTitleArtistDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="CenterTitleArtistSwitch" IsChecked="{Binding CenterTitleArtist, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="8" Icon="{ui:SymbolIcon Subtitles24}" Margin="0,0,0,12">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource ShowMediaPlayerNameTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="PlayerInfoSwitch" IsChecked="{Binding PlayerInfoEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="9" Icon="{ui:SymbolIcon ArrowRepeatAll24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource RepeatButtonTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource RepeatButtonDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="RepeatSwitch" IsChecked="{Binding RepeatEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="10" Icon="{ui:SymbolIcon ArrowShuffle24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource ShuffleButtonTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource ShuffleButtonDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="ShuffleSwitch" IsChecked="{Binding ShuffleEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="11" Icon="{ui:SymbolIcon SlideArrowRight24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource ShowSeekbarTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource ShowSeekbarDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="SeekbarSwitch" IsChecked="{Binding SeekbarEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:InfoBar Grid.Row="12" Title="{DynamicResource UnsupportedControlsWarning}" IsOpen="True" Severity="Warning" IsClosable="False" Margin="0,0,0,12"/>
                        <ui:CardControl Grid.Row="13" Icon="{ui:SymbolIcon Pause24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource PauseOtherMediaTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource PauseOtherMediaDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="PauseOtherSessionsEnabledSwitch" IsChecked="{Binding PauseOtherSessionsEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                    </Grid>

                    <TextBlock Text="{DynamicResource TaskbarWidgetCustomizationTitle}" FontSize="20" FontWeight="SemiBold" FontFamily="Segoe UI Variable" Margin="0,36,0,0"/>
                    <Grid Margin="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border CornerRadius="10" ClipToBounds="True" Width="164" Height="98">
                                <Border.Background>
                                    <ImageBrush ImageSource="/Resources/FluentFlyoutDemo9.1.png" Stretch="UniformToFill"/>
                                </Border.Background>
                            </Border>
                            <TextBlock FontSize="14" FontWeight="Regular" HorizontalAlignment="Stretch" Opacity="0.75" Margin="12,0,0,0" MaxWidth="450" TextWrapping="WrapWithOverflow" Text="{DynamicResource TaskbarWidgetDescription}"/>
                        </StackPanel>

                        <!-- Premium Status InfoBar (only shown for Store versions) -->
                        <ui:InfoBar Grid.Row="1" Title="{DynamicResource PremiumUnlockedMessage}" Severity="Success" IsClosable="True" Margin="0,0,0,3">
                            <ui:InfoBar.Style>
                                <Style TargetType="ui:InfoBar" BasedOn="{StaticResource {x:Type ui:InfoBar}}">
                                    <Setter Property="IsOpen" Value="False" />
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsStoreVersion}" Value="True"/>
                                                <Condition Binding="{Binding IsPremiumUnlocked}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="IsOpen" Value="True"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:InfoBar.Style>
                        </ui:InfoBar>

                        <!-- Premium Locked InfoBar (only shown for Store versions without premium) -->
                        <ui:InfoBar Grid.Row="1" IsOpen="True" Severity="Warning" IsClosable="False" Margin="0,0,0,3">
                            <ui:InfoBar.Style>
                                <Style TargetType="ui:InfoBar">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsStoreVersion}" Value="True"/>
                                                <Condition Binding="{Binding IsPremiumUnlocked}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:InfoBar.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource PremiumLockedMessage}" VerticalAlignment="Center"/>
                                <controls:Button Name="UnlockPremiumButton" Content="{DynamicResource UnlockPremiumButton}" 
                                               Click="UnlockPremiumButton_Click" 
                                               Style="{StaticResource MicaWPF.Styles.AccentedButton}"
                                               Margin="12,0,0,0" Padding="12,4,12,4" FontSize="12" Height="28"/>
                            </StackPanel>
                        </ui:InfoBar>

                        <!-- GitHub Version InfoBar -->
                        <ui:InfoBar Grid.Row="1" Title="{DynamicResource PremiumGitHubMessage}" IsOpen="True" Severity="Informational" IsClosable="True" Margin="0,0,0,3">
                            <ui:InfoBar.Style>
                                <Style TargetType="ui:InfoBar" BasedOn="{StaticResource {x:Type ui:InfoBar}}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsStoreVersion}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:InfoBar.Style>
                        </ui:InfoBar>

                        <ui:CardControl Grid.Row="2" Icon="{ui:SymbolIcon TabAdd24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{DynamicResource TaskbarWidgetEnabledTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                        <ui:InfoBadge Value=" Premium " FontWeight="Medium" FontSize="10" Margin="6,-2,0,-2" Severity="Attention"/>
                                    </StackPanel>
                                    <TextBlock Text="{DynamicResource TaskbarWidgetEnabledDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetEnabled, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}"/>
                        </ui:CardControl>
                        <ui:InfoBar Grid.Row="3" Title="{DynamicResource TaskbarWidgetVisualIssuesWarning}" IsOpen="True" Severity="Warning" IsClosable="False" Margin="0,0,0,3"/>

                        <ui:CardControl Grid.Row="4" Icon="{ui:SymbolIcon ArrowMove24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource TaskbarWidgetPosition}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <ComboBox SelectedIndex="{Binding TaskbarWidgetPosition}" IsEnabled="{Binding IsPremiumUnlocked}">
                                <ComboBoxItem Content="{DynamicResource PositionBottomLeft}"/>
                                <ComboBoxItem Content="{DynamicResource PositionBottomCenter}"/>
                                <ComboBoxItem Content="{DynamicResource PositionBottomRight}"/>
                            </ComboBox>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="5" Icon="{ui:SymbolIcon PaddingRight24}" Margin="0,0,0,12">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource TaskbarWidgetPaddingTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource TaskbarWidgetPaddingDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetPadding, Mode=TwoWay}">
                                <controls:ToggleSwitch.Style>
                                    <!-- Use BasedOn to keep the original look of the ToggleSwitch -->
                                    <Style TargetType="controls:ToggleSwitch" BasedOn="{StaticResource {x:Type controls:ToggleSwitch}}">
                                        <Setter Property="IsEnabled" Value="False" />
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsPremiumUnlocked}" Value="True" />
                                                    <Condition Binding="{Binding TaskbarWidgetPosition}" Value="0" />
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="IsEnabled" Value="True" />
                                            </MultiDataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsPremiumUnlocked}" Value="True" />
                                                    <Condition Binding="{Binding TaskbarWidgetPosition}" Value="2" />
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="IsEnabled" Value="True" />
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </controls:ToggleSwitch.Style>
                            </controls:ToggleSwitch>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="6" Icon="{ui:SymbolIcon ExpandUpRight24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource TaskbarWidgetClickableTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource TaskbarWidgetClickableDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <ComboBox SelectedIndex="{Binding TaskbarWidgetTriggerType, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}">
                                <ComboBoxItem Content="{DynamicResource TaskbarWidgetTrigger_None}"/>
                                <ComboBoxItem Content="{DynamicResource TaskbarWidgetTrigger_OnHover}"/>
                                <ComboBoxItem Content="{DynamicResource TaskbarWidgetTrigger_Clicked}"/>
                            </ComboBox>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="7" Icon="{ui:SymbolIcon ColorBackground24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource TaskbarWidgetBackgroundBlurTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource TaskbarWidgetBackgroundBlurDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetBackgroundBlur, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="8" Icon="{ui:SymbolIcon EyeOff24}" Margin="0,0,0,12">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource TaskbarWidgetHideCompletelyTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource TaskbarWidgetHideCompletelyDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetHideCompletely, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="9" Icon="{ui:SymbolIcon VideoPlayPause24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{DynamicResource TaskbarWidgetControlsTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                        <ui:InfoBadge Value=" New " FontWeight="Medium" FontSize="10" Margin="6,-2,0,-2" Severity="Success"/>
                                    </StackPanel>
                                    <TextBlock Text="{DynamicResource TaskbarWidgetControlsDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetControlsEnabled, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="10" Icon="{ui:SymbolIcon Wand24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{DynamicResource TaskbarWidgetAnimatedTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                        <ui:InfoBadge Value=" New " FontWeight="Medium" FontSize="10" Margin="6,-2,0,-2" Severity="Success"/>
                                    </StackPanel>
                                    <TextBlock Text="{DynamicResource TaskbarWidgetAnimatedDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch IsChecked="{Binding TaskbarWidgetAnimated, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}"/>
                        </ui:CardControl>
                    </Grid>

                    <TextBlock Text="{DynamicResource NextUpCustomizationTitle}" FontSize="20" FontWeight="SemiBold" FontFamily="Segoe UI Variable" Margin="0,36,0,0"/>
                    <Grid Margin="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border CornerRadius="10" ClipToBounds="True" Width="164" Height="98">
                                <Border.Background>
                                    <ImageBrush ImageSource="/Resources/FluentFlyoutDemo4.1.png" Stretch="UniformToFill"/>
                                </Border.Background>
                            </Border>
                            <TextBlock FontSize="14" FontWeight="Regular" HorizontalAlignment="Stretch" Opacity="0.75" Margin="12,0,0,0" MaxWidth="450" TextWrapping="WrapWithOverflow" Text="{DynamicResource NextUpDescription}"/>
                        </StackPanel>
                        <ui:CardControl Grid.Row="1" Icon="{ui:SymbolIcon MusicNote2Play20}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource EnableNextUpTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource EnableNextUpDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="NextUpSwitch" IsChecked="{Binding NextUpEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="2" Icon="{ui:SymbolIcon Blur24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource AcrylicWindowTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource AcrylicWindowDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="NextUpAcrylicWindowSwitch"  IsChecked="{Binding NextUpAcrylicWindowEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="3" Icon="{ui:SymbolIcon Timer24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource NextUpStayDurationTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource NextUpStayDurationDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <StackPanel Orientation="Horizontal">
                                <ui:TextBox Name="NextUpDurationTextBox" Width="60" ClearButtonEnabled="False" Text="{Binding NextUpDurationText, Mode=TwoWay}"/>
                                <TextBlock Text="{DynamicResource MillisecondsUnit}" FontSize="14" FontWeight="Regular" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="4" Icon="{ui:SymbolIcon CubeQuick24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource NextUpAdvancedAnimation}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource NextUpAdvancedAnimationDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="NextUpAdvancedAnimationSwitch"  IsChecked="{Binding IsNextUpAdvancedAnimationEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                    </Grid>

                    <TextBlock Text="{DynamicResource LockKeysCustomizationTitle}" FontSize="20" FontWeight="SemiBold" FontFamily="Segoe UI Variable" Margin="0,36,0,0"/>
                    <Grid Margin="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border CornerRadius="10" ClipToBounds="True" Width="164" Height="98">
                                <Border.Background>
                                    <ImageBrush ImageSource="/Resources/FluentFlyoutDemo5.1.png" Stretch="UniformToFill"/>
                                </Border.Background>
                            </Border>
                            <TextBlock FontSize="14" FontWeight="Regular" HorizontalAlignment="Stretch" Opacity="0.75" Margin="12,0,0,0" MaxWidth="450" TextWrapping="WrapWithOverflow" Text="{DynamicResource LockKeysDescription}"/>
                        </StackPanel>
                        <ui:CardControl Grid.Row="1" Icon="{ui:SymbolIcon LockMultiple24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource EnableLockKeysTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource EnableLockKeysDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="LockKeysSwitch" IsChecked="{Binding LockKeysEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="2" Icon="{ui:SymbolIcon Blur24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource AcrylicWindowTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource AcrylicWindowDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="LockKeysAcrylicWindowSwitch" IsChecked="{Binding LockKeysAcrylicWindowEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="3" Icon="{ui:SymbolIcon Timer24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource LockKeysStayDurationTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource LockKeysStayDurationDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <StackPanel Orientation="Horizontal">
                                <ui:TextBox Name="LockKeysDurationTextBox" Width="60" ClearButtonEnabled="False" Text="{Binding LockKeysDurationText, Mode=TwoWay}"/>
                                <TextBlock Text="{DynamicResource MillisecondsUnit}" FontSize="14" FontWeight="Regular" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="4" Icon="{ui:SymbolIcon TextBold24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource LockKeysBoldUITitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource LockKeysBoldUIDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="LockKeysBoldUISwitch" IsChecked="{Binding LockKeysBoldUi, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="5" Icon="{ui:SymbolIcon Info24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource EnableInsertKeyTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource EnableInsertKeyDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="LockKeysEnableInsertSwitch" IsChecked="{Binding LockKeysInsertEnabled, Mode=TwoWay}"/>
                        </ui:CardControl>
                    </Grid>

                    <TextBlock Text="{DynamicResource SystemSettingsTitle}" FontSize="20" FontWeight="SemiBold" Margin="0,36,0,0"/>
                    <Grid Margin="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ui:CardControl Grid.Row="0" Icon="{ui:SymbolIcon ShareScreenStart24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource LaunchOnStartupTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource LaunchOnStartupDescription}" FontSize="12" Opacity="0.5"/>
                                    <TextBlock VerticalAlignment="Center" FontSize="12" Opacity="0.5">
                                        <Run Text="{DynamicResource StartupAppsSettingsInfo}"/>
                                        <Hyperlink Name="StartupHyperlink" NavigateUri="ms-settings:startupapps" BaselineAlignment="Center"
                                                   RequestNavigate="StartupHyperlink_RequestNavigate" TextDecorations="None" Foreground="{DynamicResource MicaWPF.Brushes.AccentFillColorDefault}">
                                            <Run Text="{DynamicResource StartupAppsHyperlinkText}"/> <ui:SymbolIcon VerticalAlignment="Center" FontSize="14" Symbol="Apps16"/>
                                        </Hyperlink>
                                    </TextBlock>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="StartupSwitch" Click="StartupSwitch_Click" IsChecked="{Binding Startup, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="1" Icon="{ui:SymbolIcon Color24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource AppThemeTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <ComboBox Name="AppThemeComboBox" SelectedIndex="{Binding AppTheme, Mode=TwoWay}" IsReadOnly="False">
                                <ComboBoxItem Content="{DynamicResource AppThemeDefault}"/>
                                <ComboBoxItem Content="{DynamicResource AppThemeLight}"/>
                                <ComboBoxItem Content="{DynamicResource AppThemeDark}"/>
                            </ComboBox>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="2" Icon="{ui:SymbolIcon Globe24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource AppLanguageTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock VerticalAlignment="Center" FontSize="12" Opacity="0.5">
                                        <Run Text="{DynamicResource AppLanguageDescription}"/>
                                        <Hyperlink NavigateUri="https://hosted.weblate.org/engage/fluentflyout/" RequestNavigate="StartupHyperlink_RequestNavigate" BaselineAlignment="Center"
                                            TextDecorations="None" Foreground="{DynamicResource MicaWPF.Brushes.AccentFillColorDefault}">
                                            <Run Text="{DynamicResource AppLanguageDescriptionLink}"/> <ui:SymbolIcon VerticalAlignment="Center" FontSize="14" Symbol="Open16"/>
                                        </Hyperlink>
                                    </TextBlock>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <StackPanel Orientation="Horizontal">
                                <ComboBox Name="AppLanguageComboBox" 
                                          ItemsSource="{Binding LanguageOptions}"
                                          SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}"
                                          DisplayMemberPath="DisplayName" />
                            </StackPanel>
                        </ui:CardControl>
                        <ui:CardExpander Grid.Row="4" Icon="{ui:SymbolIcon Wrench24}" ContentPadding="6" Margin="0,0,0,3">
                            <ui:CardExpander.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource AnimationSettingsTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource AnimationSettingsDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardExpander.Header>
                            <StackPanel>
                                <ui:CardControl Icon="{ui:SymbolIcon TopSpeed24}" Margin="0,0,0,3">
                                    <ui:CardControl.Header>
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock Text="{DynamicResource AnimationDurationScaleTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                            <TextBlock Text="{DynamicResource AnimationDurationScaleDescription}" FontSize="12" Opacity="0.5"/>
                                        </StackPanel>
                                    </ui:CardControl.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <ComboBox Name="FlyoutAnimationSpeedComboBox" SelectedIndex="{Binding FlyoutAnimationSpeed, Mode=TwoWay}">
                                            <ComboBoxItem Content="{DynamicResource AnimationSpeedOff}"/>
                                            <ComboBoxItem Content="0.5x"/>
                                            <ComboBoxItem Content="1x"/>
                                            <ComboBoxItem Content="1.5x"/>
                                            <ComboBoxItem Content="2x"/>
                                            <ComboBoxItem Content="3x"/>
                                        </ComboBox>
                                    </StackPanel>
                                </ui:CardControl>
                                <ui:CardControl Icon="{ui:SymbolIcon BezierCurveSquare12}" Margin="0,0,0,3">
                                    <ui:CardControl.Header>
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock Text="{DynamicResource AnimationEasingStyleTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                            <TextBlock Text="{DynamicResource AnimationEasingStyleDescription}" FontSize="12" Opacity="0.5"/>
                                        </StackPanel>
                                    </ui:CardControl.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <ComboBox Name="AnimationEasingStylesComboBox" SelectedIndex="{Binding FlyoutAnimationEasingStyle, Mode=TwoWay}">
                                            <ComboBoxItem Content="{DynamicResource EasingLinear}"/>
                                            <ComboBoxItem Content="{DynamicResource EasingSine}"/>
                                            <ComboBoxItem Content="{DynamicResource EasingQuadratic}"/>
                                            <ComboBoxItem Content="{DynamicResource EasingCubic}"/>
                                        </ComboBox>
                                    </StackPanel>
                                </ui:CardControl>
                                <ui:InfoBar Title="{DynamicResource NativeExperienceInfo}" IsOpen="True" Severity="Informational" IsClosable="False"/>
                            </StackPanel>
                        </ui:CardExpander>
                        <ui:CardControl Grid.Row="5" Icon="{ui:SymbolIcon ArrowMinimize24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource DisableOnFullscreenTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource DisableOnFullscreenDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="DisableIfFullscreenSwitch" IsChecked="{Binding DisableIfFullscreen, Mode=TwoWay}"/>
                        </ui:CardControl>

                        <!-- tray icon section -->
                        <TextBlock Grid.Row="6" Text="{DynamicResource TrayIconSectionTitle}" FontSize="14" FontWeight="SemiBold" Margin="0,12,0,6"/>
                        <ui:CardControl Grid.Row="7" Icon="{ui:SymbolIcon CursorHover24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource TrayIconLeftClickBehaviorTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource TrayIconLeftClickBehaviorDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <StackPanel Orientation="Horizontal">
                                <ComboBox SelectedIndex="{Binding NIconLeftClick, Mode=TwoWay}">
                                    <ComboBoxItem Content="{DynamicResource TrayIconLeftClickOption1}"/>
                                    <ComboBoxItem Content="{DynamicResource TrayIconLeftClickOption2}"/>
                                </ComboBox>
                            </StackPanel>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="8" Icon="{ui:SymbolIcon PaintBrush24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{DynamicResource Win11TrayIconTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                    <TextBlock Text="{DynamicResource Win11TrayIconDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch IsChecked="{Binding NIconSymbol, Mode=TwoWay}"/>
                        </ui:CardControl>
                        <ui:CardControl Grid.Row="9" Icon="{ui:SymbolIcon EyeOff24}" Margin="0,0,0,3">
                            <ui:CardControl.Header>
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{DynamicResource HideTrayIconTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center"/>
                                        <ui:InfoBadge Value=" Premium " FontSize="10" FontWeight="Medium" Margin="6,-2,0,-2" Severity="Attention"/>
                                    </StackPanel>
                                    <TextBlock Text="{DynamicResource HideTrayIconDescription}" FontSize="12" Opacity="0.5"/>
                                </StackPanel>
                            </ui:CardControl.Header>
                            <controls:ToggleSwitch Name="NIconHideSwitch" IsChecked="{Binding NIconHide, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}" Click="ToggleSwitch_Click"/>
                        </ui:CardControl>
                    </Grid>

                    <TextBlock Text="{DynamicResource AboutPremiumTitle}" FontSize="20" FontWeight="SemiBold" FontFamily="Segoe UI Variable" Margin="0,36,0,0"/>
                    <Grid Margin="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <!-- Premium Status InfoBar (only shown for Store versions) -->
                        <ui:InfoBar Grid.Row="1" Title="{DynamicResource PremiumUnlockedMessage}" Severity="Success" IsClosable="False" Margin="0,0,0,12">
                            <ui:InfoBar.Style>
                                <Style TargetType="ui:InfoBar" BasedOn="{StaticResource {x:Type ui:InfoBar}}">
                                    <Setter Property="IsOpen" Value="False" />
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsStoreVersion}" Value="True"/>
                                                <Condition Binding="{Binding IsPremiumUnlocked}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="IsOpen" Value="True"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:InfoBar.Style>
                        </ui:InfoBar>

                        <!-- Premium Locked InfoBar (only shown for Store versions without premium) -->
                        <ui:InfoBar Grid.Row="1" IsOpen="True" Severity="Warning" IsClosable="False" Margin="0,0,0,12">
                            <ui:InfoBar.Style>
                                <Style TargetType="ui:InfoBar">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsStoreVersion}" Value="True"/>
                                                <Condition Binding="{Binding IsPremiumUnlocked}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:InfoBar.Style>
                            <StackPanel Orientation="Horizontal">
                                <controls:Button Content="{DynamicResource UnlockPremiumButton}" 
                       Click="UnlockPremiumButton_Click" 
                       Style="{StaticResource MicaWPF.Styles.AccentedButton}"
                       Margin="12,0,0,0" Padding="12,4,12,4" FontSize="12" Height="28"/>
                            </StackPanel>
                        </ui:InfoBar>

                        <!-- GitHub Version InfoBar -->
                        <ui:InfoBar Grid.Row="1" Title="{DynamicResource PremiumGitHubMessage}" IsOpen="True" Severity="Informational" IsClosable="False" Margin="0,0,0,12">
                            <ui:InfoBar.Style>
                                <Style TargetType="ui:InfoBar" BasedOn="{StaticResource {x:Type ui:InfoBar}}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsStoreVersion}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:InfoBar.Style>
                        </ui:InfoBar>

                        <TextBlock Grid.Row="2" FontSize="14" Margin="4,0,0,0" FontWeight="Regular" HorizontalAlignment="Left" Opacity="0.75" MaxWidth="600" TextWrapping="WrapWithOverflow" Text="{DynamicResource PremiumFeatureDescription}"/>
                    </Grid>
                </StackPanel>

                <DockPanel LastChildFill="False">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Right">
                        <ui:SymbolIcon Symbol="Star24" Filled="True" Width="24" Height="24" FontSize="16" VerticalAlignment="Center" Foreground="Gold"/>
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="{DynamicResource EnjoyingAppText}" HorizontalAlignment="Center" FontSize="12" Opacity="0.5" VerticalAlignment="Center" Margin="4,0,0,0"/>
                        </StackPanel>
                        <ui:Anchor Content="{DynamicResource GitHubRepoLink}" Icon="{ui:SymbolIcon Open24}" NavigateUri="https://github.com/unchihugo/FluentFlyout" Margin="12,0,0,0"/>
                        <controls:Button Name="SaveButton" Style="{StaticResource MicaWPF.Styles.AccentedButton}" Content="{DynamicResource CloseButton}" Padding="12,0,12,0" Height="32" Click="SaveButton_Click" Margin="8,24,24,24" HorizontalAlignment="Left" FontWeight="SemiBold"/>
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </ui:PassiveScrollViewer>
    </Grid>
</controls:MicaWindow>
