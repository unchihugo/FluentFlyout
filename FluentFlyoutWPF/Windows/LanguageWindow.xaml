<controls:MicaWindow x:Class="FluentFlyoutWPF.Windows.LanguageWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
    xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
    mc:Ignorable="d" 
    Height="50" MinWidth="160"
    ResizeMode="NoResize"
    WindowStyle="None" ShowInTaskbar="False"
    ChangeTitleColorWhenInactive="False" Visibility="Visible"
    TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Ideal"
    FontFamily="{Binding FontFamily}"
    FlowDirection="{Binding FlowDirection}"
    d:DataContext="{d:DesignInstance viewModels:UserSettings}"
    Background="Transparent"
    >
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <EventTrigger.Actions>
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(Window.Top)">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(Window.Opacity)">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger.Actions>
        </EventTrigger>
    </Window.Triggers>

    <Grid x:Name="RootGrid" Margin="14,6,14,6">
        <StackPanel x:Name="ContentStack" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,4">
            <Grid Name="LangIconGrid" Width="24" Height="24" VerticalAlignment="Center">
                 <Ellipse Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" StrokeThickness="1.5"/>
                 <TextBlock Name="LangShortText" Text="??" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,1"/>
            </Grid>
            
            <TextBlock Name="LangFullText" Text="Language" FontSize="14" FontWeight="Medium" TextTrimming="None" TextWrapping="NoWrap" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
        
        <Rectangle Name="AccentIndicator" Fill="{DynamicResource MicaWPF.Brushes.AccentFillColorDefault}" Height="4" RadiusX="2" RadiusY="2" Width="60" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
    </Grid>
</controls:MicaWindow>
