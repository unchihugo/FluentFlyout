<controls:MicaWindow x:Class="FluentFlyoutWPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:tray="http://schemas.lepo.co/wpfui/2022/xaml/tray"
    xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
    xmlns:corecontrols="clr-namespace:MicaWPF.Core.Controls;assembly=MicaWPF.Core"
    Height="116" Width="310" Top="-9999"
    WindowStyle="None" mc:Ignorable="d" ShowInTaskbar="False"
    ResizeMode="NoResize" MouseEnter="MicaWindow_MouseEnter"
    ChangeTitleColorWhenInactive="False"
    TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Ideal" 
    FontFamily="{Binding FontFamily}"
    FlowDirection="{Binding FlowDirection}"
    Loaded="MicaWindow_Loaded"
    xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:UserSettings}"
    >
    <controls:MicaWindow.Resources>
        <!--
            Same appearance as MicaWPF.Styles.AccentedButton, but the hover/pressed states dim the accent
            color instead of animating it to the system accent shade, so the album art accent color
            (ControlPlayPause.Background) is preserved in every state.
        -->
        <Style x:Key="PlayPauseButtonStyle" TargetType="{x:Type controls:Button}"
               BasedOn="{StaticResource MicaWPF.Styles.AccentedButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type controls:Button}">
                        <Grid>
                            <corecontrols:GradientBorder
                                x:Name="ContentBorder"
                                Margin="-2"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Style="{DynamicResource MicaWPF.Styles.GradientBorder.Accent}" />
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ContentBorder" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ContentBorder" Property="Opacity" Value="0.8" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- The media selector opens above the flyout, so its content should travel upward into place. -->
        <Style x:Key="MediaSessionContextMenuStyle"
               TargetType="{x:Type ContextMenu}"
               BasedOn="{StaticResource UiContextMenu}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <ui:EffectThicknessDecorator Thickness="30">
                            <Border x:Name="MenuBorder"
                                    Padding="0,3"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Opacity="0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="20"
                                                      Direction="270"
                                                      Opacity="0.135"
                                                      ShadowDepth="10"
                                                      Color="#202020"/>
                                </Border.Effect>
                                <Border.RenderTransform>
                                    <TranslateTransform/>
                                </Border.RenderTransform>
                                <StackPanel ClipToBounds="True"
                                            IsItemsHost="True"
                                            KeyboardNavigation.DirectionalNavigation="Cycle"
                                            Orientation="Vertical"/>
                            </Border>
                        </ui:EffectThicknessDecorator>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsOpen" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="MenuBorder"
                                                             Storyboard.TargetProperty="(Border.RenderTransform).(TranslateTransform.Y)"
                                                             From="10" To="0" Duration="0:0:0.167">
                                                <DoubleAnimation.EasingFunction>
                                                    <CircleEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="MenuBorder"
                                                             Storyboard.TargetProperty="Opacity"
                                                             From="0" To="1" Duration="0:0:0.12"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </controls:MicaWindow.Resources>
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <EventTrigger.Actions>
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(Window.Top)">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(Window.Opacity)">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger.Actions>
        </EventTrigger>
    </Window.Triggers>

    <Grid>
        <controls:Button x:Name="MediaFlyoutCloseButton"
                         Width="28" Height="28" 
                         Style="{StaticResource MicaWPF.Styles.TransparentButton}"
                         HorizontalAlignment="Right" VerticalAlignment="Top"
                         Margin="0,8,8,0"
                         Click="MediaFlyoutCloseButton_Click"
                         Visibility="Collapsed"  >
            <ui:SymbolIcon Symbol="Dismiss20" Filled="True"/>
        </controls:Button>
        <Grid>
            <Image x:Name="BackgroundImageStyle1" Stretch="UniformToFill" Opacity="1" IsHitTestVisible="False" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5" Visibility="Collapsed" ClipToBounds="True">
                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                    </TransformGroup>
                </Image.RenderTransform>
                <Image.Effect>
                    <BlurEffect Radius="50"/>
                </Image.Effect>
                <Image.OpacityMask>
                    <RadialGradientBrush GradientOrigin="0.2,0.5" Center="0.1,0.5" RadiusX="0.7" RadiusY="0.3">
                        <GradientStop Color="#FF000000" Offset="0.0"/>
                        <GradientStop Color="#00000080" Offset="0.75"/>
                        <GradientStop Color="#00000000" Offset="1.0"/>
                    </RadialGradientBrush>
                </Image.OpacityMask>
            </Image>
            <Image x:Name="BackgroundImageStyle2" Stretch="Fill" Opacity="0.8" IsHitTestVisible="False" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5" Visibility="Collapsed" ClipToBounds="True">
                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="1.8" ScaleY="1.8"/>
                    </TransformGroup>
                </Image.RenderTransform>
                <Image.Effect>
                    <BlurEffect Radius="200"/>
                </Image.Effect>
                <Image.OpacityMask>
                    <RadialGradientBrush GradientOrigin="0.5,1" Center="0.5,1" RadiusX="0.4" RadiusY="0.7">
                        <GradientStop Color="#FF000000" Offset="0.0"/>
                        <GradientStop Color="#00000000" Offset="1.0"/>
                    </RadialGradientBrush>
                </Image.OpacityMask>
            </Image>
            <Image x:Name="BackgroundImageStyle3" Stretch="UniformToFill" Opacity="0.3" IsHitTestVisible="False" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5" Visibility="Collapsed">
                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                    </TransformGroup>
                </Image.RenderTransform>
                <Image.Effect>
                    <BlurEffect Radius="50"/>
                </Image.Effect>
            </Image>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="12" Orientation="Horizontal">
                    <Border Name="SongImageBorder" CornerRadius="6" BorderThickness="1" BorderBrush="#26FFFFFF" Margin="0" ClipToBounds="True" Width="78" Height="78">
                        <ui:SymbolIcon Name="SongImagePlaceholder" Symbol="MusicNote220" FontSize="40" Filled="True" Foreground="{DynamicResource MicaWPF.Brushes.SystemAccentColorSecondary}" Visibility="Collapsed" />
                        <Border.Background>
                            <ImageBrush x:Name="SongImage" Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>
                    <StackPanel Name="BodyStackPanel" Orientation="Vertical" VerticalAlignment="Center" Width="194">
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Name="SongInfoStackPanel" Margin="12,0,0,0" Orientation="Vertical" VerticalAlignment="Center" Width="182"
                                        ToolTipService.InitialShowDelay="800"
                                        ToolTipService.Placement="Top"
                                        ToolTipService.VerticalOffset="-16">
                                <TextBlock Name="SongTitle" Text="Song Title" FontSize="14" FontWeight="Medium" TextTrimming="CharacterEllipsis" />
                                <TextBlock Name="SongArtist" Text="Artist Name" FontSize="14" FontWeight="Medium" Opacity="0.5" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </StackPanel>
                        <Grid Name="ControlsStackPanelContainer" HorizontalAlignment="Stretch" Height="35" Margin="12,8,0,0">
                            <StackPanel Name="ControlsStackPanel" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <!--<ui:Button Name="ControlBack" Icon="{ui:SymbolIcon Previous20, Filled=True}" Width="28" Height="28" Padding="0" CornerRadius="999" Margin="0,0,8,0" Click="Back_Click" Focusable="False"/>-->
                                <controls:Button
                                    Height="28" Width="28" Name="ControlBack"
                                    Margin="0" Click="Back_Click" Focusable="False"
                                    Style="{StaticResource MicaWPF.Styles.TransparentButton}" VerticalAlignment="Center">
                                    <ui:SymbolIcon Name="SymbolBack" Symbol="Previous20" Filled="True"/>
                                </controls:Button>
                                <controls:Button
                                    Height="32" Width="32" Name="ControlPlayPause"
                                    Margin="8,0,0,0" Click="PlayPause_Click" Focusable="False"
                                    Style="{StaticResource PlayPauseButtonStyle}" VerticalAlignment="Center">
                                    <ui:SymbolIcon Name="SymbolPlayPause" Symbol="Pause16" Filled="True"/>
                                </controls:Button>
                                <controls:Button
                                    Height="28" Width="28" Name="ControlForward"
                                    Margin="8,0,0,0" Click="Forward_Click" Focusable="False"
                                    Style="{StaticResource MicaWPF.Styles.TransparentButton}" VerticalAlignment="Center">
                                    <ui:SymbolIcon Name="SymbolForward" Symbol="Next20" Filled="True"/>
                                </controls:Button>
                                <controls:Button
                                    Width="28" Height="28" Name="ControlClose"
                                    Margin="8,0,0,0" Click="MediaFlyoutCloseButton_Click"
                                    Style="{StaticResource MicaWPF.Styles.TransparentButton}" VerticalAlignment="Center"
                                    Visibility="Collapsed">
                                    <ui:SymbolIcon Symbol="Dismiss20" Filled="True"/>
                                </controls:Button>
                                <controls:Button
                                    Height="28" Width="28" Name="ControlRepeat"
                                    Margin="8,0,0,0" Click="Repeat_Click" Focusable="False"
                                    Style="{StaticResource MicaWPF.Styles.TransparentButton}" VerticalAlignment="Center">
                                    <ui:SymbolIcon Name="SymbolRepeat" Symbol="ArrowRepeatAllOff24" Filled="false" Opacity="0.5" FontSize="18"/>
                                </controls:Button>
                                <controls:Button
                                    Height="28" Width="28" Name="ControlShuffle"
                                    Margin="8,0,0,0" Click="Shuffle_Click" Focusable="False"
                                    Style="{StaticResource MicaWPF.Styles.TransparentButton}" VerticalAlignment="Center">
                                    <ui:SymbolIcon Name="SymbolShuffle" Symbol="ArrowShuffleOff24" Filled="false" Opacity="0.5" FontSize="18"/>
                                </controls:Button>

                                <controls:Button Name="MediaIdButton" Padding="0" Height="28" Width="70"
                                    Margin="8,0,0,0"
                                    Style="{StaticResource MicaWPF.Styles.TransparentButton}"
                                    Click="MediaIdButton_Click">
                                    <StackPanel Orientation="Horizontal" Margin="2,0">
                                        <Image x:Name="MediaIdIcon" Width="14" Height="14"/>
                                        <TextBlock Name="MediaId" VerticalAlignment="Center" FontSize="10" Width="48" Margin="4,0,0,0"
                                                   FontWeight="Regular" Opacity="0.25" TextWrapping="Wrap"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </controls:Button>

                                <ui:SplitButton Name="MediaSessionSplitButton"
                                    Height="28" Width="108" Padding="4,0"
                                    Margin="8,0,0,0"
                                    Appearance="Transparent"
                                    Visibility="Collapsed"
                                    ToolTip="{DynamicResource MediaSessionSelectorToolTip}"
                                    PreviewMouseLeftButtonDown="MediaSessionSplitButton_PreviewMouseLeftButtonDown"
                                    Click="MediaSessionSplitButton_Click">
                                    <TextBlock Name="MediaSessionId" VerticalAlignment="Center" FontSize="10" Width="46"
                                               FontWeight="Regular" Opacity="0.5" TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis"/>
                                    <ui:SplitButton.Template>
                                        <ControlTemplate TargetType="{x:Type ui:SplitButton}">
                                            <Border x:Name="ContentBorder"
                                                    Width="{TemplateBinding Width}"
                                                    Height="{TemplateBinding Height}"
                                                    Background="Transparent"
                                                    BorderBrush="Transparent"
                                                    BorderThickness="1"
                                                    CornerRadius="{TemplateBinding CornerRadius}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="28"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border x:Name="PART_Content"
                                                            Grid.Column="0"
                                                            Background="Transparent">
                                                        <Grid HorizontalAlignment="Stretch"
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="28"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>
                                                            <ContentPresenter x:Name="ControlIcon"
                                                                              Grid.Column="0"
                                                                              Margin="0"
                                                                              HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center"
                                                                              Content="{TemplateBinding Icon}"
                                                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                              Focusable="False"
                                                                              TextElement.FontSize="{TemplateBinding FontSize}"
                                                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                                                            <ContentPresenter x:Name="ContentPresenter"
                                                                              Grid.Column="1"
                                                                              HorizontalAlignment="Left"
                                                                              VerticalAlignment="Center"
                                                                              Content="{TemplateBinding Content}"
                                                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                                                        </Grid>
                                                    </Border>
                                                    <Border x:Name="PART_Toggle"
                                                            Grid.Column="1"
                                                            Background="Transparent"
                                                            CornerRadius="0,3,3,0">
                                                        <ToggleButton x:Name="PART_ToggleButton"
                                                                      HorizontalAlignment="Stretch"
                                                                      VerticalAlignment="Stretch"
                                                                      ClickMode="Press"
                                                                      Focusable="False"
                                                                      Foreground="{TemplateBinding Foreground}"
                                                                      IsChecked="{TemplateBinding IsDropDownOpen}">
                                                            <ToggleButton.Template>
                                                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                                    <Border Background="Transparent">
                                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                                          VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </ToggleButton.Template>
                                                            <ui:SymbolIcon x:Name="DropDownGlyph"
                                                                           FontSize="10"
                                                                           Opacity="0.65"
                                                                           Symbol="ChevronDown24"/>
                                                        </ToggleButton>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition SourceName="PART_Toggle" Property="IsMouseOver" Value="True"/>
                                                        <Condition Property="IsPressed" Value="False"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="ContentBorder" Property="BorderBrush"
                                                            Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                                    <Setter TargetName="PART_Toggle" Property="Background"
                                                            Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                    <Setter TargetName="DropDownGlyph" Property="Opacity" Value="1"/>
                                                </MultiTrigger>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition SourceName="PART_Content" Property="IsMouseOver" Value="True"/>
                                                        <Condition Property="IsPressed" Value="False"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="ContentBorder" Property="Background"
                                                            Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                    <Setter TargetName="ContentBorder" Property="BorderBrush"
                                                            Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                                </MultiTrigger>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition SourceName="PART_ToggleButton" Property="IsPressed" Value="True"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="ContentBorder" Property="BorderBrush"
                                                            Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                                    <Setter TargetName="PART_Toggle" Property="Background"
                                                            Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                    <Setter TargetName="DropDownGlyph" Property="Opacity" Value="1"/>
                                                </MultiTrigger>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition SourceName="PART_Toggle" Property="IsMouseOver" Value="False"/>
                                                        <Condition Property="IsPressed" Value="True"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="ContentBorder" Property="Background"
                                                            Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                    <Setter TargetName="ContentPresenter" Property="TextElement.Foreground"
                                                            Value="{Binding PressedForeground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                    <Setter TargetName="ControlIcon" Property="TextElement.Foreground"
                                                             Value="{Binding PressedForeground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                </MultiTrigger>
                                                <Trigger Property="IsDropDownOpen" Value="True">
                                                    <Setter TargetName="ContentBorder" Property="Background"
                                                            Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                    <Setter TargetName="ContentBorder" Property="BorderBrush"
                                                            Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                                    <Setter TargetName="PART_Toggle" Property="Background"
                                                            Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                    <Setter TargetName="DropDownGlyph" Property="Opacity" Value="1"/>
                                                </Trigger>
                                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                                    <Setter TargetName="ContentBorder" Property="BorderBrush"
                                                            Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter TargetName="ContentBorder" Property="Background"
                                                            Value="{DynamicResource ButtonBackgroundDisabled}"/>
                                                    <Setter TargetName="ContentBorder" Property="BorderBrush"
                                                            Value="{DynamicResource ButtonBorderBrushDisabled}"/>
                                                    <Setter TargetName="ContentPresenter" Property="TextElement.Foreground"
                                                            Value="{DynamicResource ButtonForegroundDisabled}"/>
                                                </Trigger>
                                                <Trigger Property="Content" Value="{x:Null}">
                                                    <Setter TargetName="ControlIcon" Property="Margin" Value="0"/>
                                                </Trigger>
                                                <Trigger Property="Content" Value="">
                                                    <Setter TargetName="ControlIcon" Property="Margin" Value="0"/>
                                                </Trigger>
                                                <Trigger Property="Icon" Value="{x:Null}">
                                                    <Setter TargetName="ControlIcon" Property="Margin" Value="0"/>
                                                    <Setter TargetName="ControlIcon" Property="Visibility" Value="Collapsed"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ui:SplitButton.Template>
                                    <ui:SplitButton.Flyout>
                                        <ContextMenu Width="280" Style="{StaticResource MediaSessionContextMenuStyle}"
                                                     Opened="MediaSessionMenu_Opened"
                                                     Closed="MediaSessionMenu_Closed"/>
                                    </ui:SplitButton.Flyout>
                                </ui:SplitButton>

                                <ui:DropDownButton Name="CompactMediaSessionButton"
                                    Height="28" Width="56" Padding="0"
                                    Margin="8,0,0,0"
                                    Appearance="Transparent"
                                    Visibility="Collapsed"
                                    ToolTip="{DynamicResource MediaSessionSelectorToolTip}">
                                    <ui:DropDownButton.Template>
                                        <ControlTemplate TargetType="{x:Type ui:DropDownButton}">
                                            <Border x:Name="ContentBorder"
                                                    Width="{TemplateBinding Width}"
                                                    Height="{TemplateBinding Height}"
                                                    Background="{TemplateBinding Background}"
                                                    CornerRadius="{TemplateBinding CornerRadius}">
                                                <Border BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="{TemplateBinding CornerRadius}">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="28"/>
                                                            <ColumnDefinition Width="28"/>
                                                        </Grid.ColumnDefinitions>
                                                        <ContentPresenter x:Name="ControlIcon"
                                                                          Grid.Column="0"
                                                                          HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"
                                                                          Content="{TemplateBinding Icon}"
                                                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                          Focusable="False"
                                                                          TextElement.FontSize="{TemplateBinding FontSize}"
                                                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                                                        <ui:SymbolIcon Grid.Column="1"
                                                                       HorizontalAlignment="Center"
                                                                       VerticalAlignment="Center"
                                                                       FontSize="10"
                                                                       Symbol="ChevronDown24"/>
                                                    </Grid>
                                                </Border>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition Property="IsMouseOver" Value="True"/>
                                                        <Condition Property="IsPressed" Value="False"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="ContentBorder" Property="Background"
                                                            Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                </MultiTrigger>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition Property="IsMouseOver" Value="True"/>
                                                        <Condition Property="IsPressed" Value="True"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="ContentBorder" Property="Background"
                                                            Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                    <Setter TargetName="ControlIcon" Property="TextElement.Foreground"
                                                            Value="{Binding PressedForeground, RelativeSource={RelativeSource TemplatedParent}}"/>
                                                </MultiTrigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter TargetName="ContentBorder" Property="Background"
                                                            Value="{DynamicResource ControlFillColorDisabledBrush}"/>
                                                    <Setter TargetName="ContentBorder" Property="BorderBrush"
                                                            Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ui:DropDownButton.Template>
                                    <ui:DropDownButton.Flyout>
                                        <ContextMenu Width="280" Style="{StaticResource MediaSessionContextMenuStyle}"
                                                     Opened="MediaSessionMenu_Opened"
                                                     Closed="MediaSessionMenu_Closed"/>
                                    </ui:DropDownButton.Flyout>
                                </ui:DropDownButton>

                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </StackPanel>
                <DockPanel Grid.Row="1" x:Name="SeekbarWrapper" HorizontalAlignment="Stretch" Margin="16,0,16,12" LastChildFill="True" VerticalAlignment="Center">
                    <TextBlock Name="SeekbarCurrentDuration" Text="-:--" Opacity="0.5" FontSize="12" DockPanel.Dock="Left" Width="45" Margin="0,0,2,0" TextAlignment="Left" VerticalAlignment="Center" Height="18"/>
                    <TextBlock Name="SeekbarMaxDuration" Text="-:--" Opacity="0.5" FontSize="12" DockPanel.Dock="Right" Width="45" Margin="2,0,0,0" TextAlignment="Right" VerticalAlignment="Center" Height="18"/>
                    <Slider Name="Seekbar" Value="0" Maximum="100" Minimum="0" HorizontalAlignment="Stretch" VerticalAlignment="Center" VerticalContentAlignment="Center" Height="18"
                            IsMoveToPointEnabled="True"
                            PreviewMouseLeftButtonDown="Seekbar_OnPreviewMouseLeftButtonDown"
                            PreviewMouseLeftButtonUp="Seekbar_OnPreviewMouseLeftButtonUp"
                            ValueChanged="Seekbar_OnValueChanged"/>
                </DockPanel>
            </Grid>
        </Grid>

        <tray:NotifyIcon
            LeftClick="nIcon_LeftClick" FocusOnLeftClick="False"
            x:Name="nIcon"
            MenuOnRightClick="True"
            TooltipText="FluentFlyout"
            Icon="/Resources/FluentFlyout2.ico"
            Margin="0,127,0,114" Visibility="Collapsed">
            <tray:NotifyIcon.Menu>
                <ContextMenu>
                    <ui:MenuItem x:Name="NotifyIconSettings" Header="{DynamicResource TrayIcon_SettingsOption}" Click="openSettings">
                        <ui:MenuItem.Icon>
                            <ui:SymbolIcon Symbol="Settings20"/>
                        </ui:MenuItem.Icon>
                    </ui:MenuItem>
                    <Separator></Separator>
                    <ui:MenuItem x:Name="NotifyIconRepository" Header="{DynamicResource TrayIcon_GitHubRepositoryOption}" Click="openRepository">
                        <ui:MenuItem.Icon>
                            <ui:SymbolIcon Symbol="DocumentChevronDouble20"/>
                        </ui:MenuItem.Icon>
                    </ui:MenuItem>
                    <ui:MenuItem x:Name="NotifyIconViewLogs" Header="{DynamicResource TrayIcon_ViewLogsOption}" Click="openLogsFolder">
                        <ui:MenuItem.Icon>
                            <ui:SymbolIcon Symbol="FolderOpen20"/>
                        </ui:MenuItem.Icon>
                    </ui:MenuItem>
                    <ui:MenuItem x:Name="NotifyIconReportBug" Header="{DynamicResource TrayIcon_ReportBugOption}" Click="reportBug">
                        <ui:MenuItem.Icon>
                            <ui:SymbolIcon Symbol="Bug20"/>
                        </ui:MenuItem.Icon>
                    </ui:MenuItem>
                    <Separator></Separator>
                    <ui:MenuItem x:Name="NotifyIconQuit" Header="{DynamicResource TrayIcon_QuitOption}" Click="NotifyIconQuit_Click">
                        <ui:MenuItem.Icon>
                            <ui:SymbolIcon Symbol="ArrowExit20"/>
                        </ui:MenuItem.Icon>
                    </ui:MenuItem>
                </ContextMenu>
            </tray:NotifyIcon.Menu>
        </tray:NotifyIcon>
    </Grid>
</controls:MicaWindow>
